# 交互记录 05：Grover 位序归一化

- 日期：2026-07-27
- 现象：请求目标状态 `10110`，算法日志显示找到 `01101`，两者恰好反序。
- Agent 分析：UnitaryLab Algorithms 的该输出使用后端比特展示顺序，而用户协议使用从左到右的规范二进制串。
- 修复：结果中保留 `backend_bit_order_state` 原始值，同时生成规范 `found_state`；所有目标匹配和报告基于规范值。
- 独立复测：规范结果为 `10110`，实测目标概率 0.99918133020401，理论误差 9.853e-07，CPU/GPU 概率差为 0。
- 结果：避免了量子软件常见的端序误判，并保留可审计的原始字段。

